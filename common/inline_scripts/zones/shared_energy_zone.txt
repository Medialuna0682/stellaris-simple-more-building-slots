# Simple More Building Slots: slots -> @TINYGROX_MAX_BUILDINGS, max_buildings cap added if missing (vanilla shared_energy_zone.txt).
icon = GFX_district_specialization_energy
base_buildtime = @zone_buildtime

resources = {
	category = planet_zones
	inline_script = {
		script = buildings/nomadic_cost_switcher
		REGULAR_RESOURCE = minerals
		NOMADIC_RESOURCE = alloys
		COST = @zone_cost
		NOMADIC_COST_MULT = 0.5
	}
}

potential = { # planet scope
	hidden_trigger = {
		exists = owner
		owner = {
			is_wilderness_empire = no
		}
	}
}

unlock = { # planet scope
	hidden_trigger = {
		exists = owner
		owner = {
			is_wilderness_empire = no
		}
	}
}

included_building_sets = {
	generator
	fallen_empire_generator
	generator_automation
}

convert_to = {
	zone_energy
	zone_energy_nexus
	zone_energy_hive
}

inline_script = {
	script = jobs/zone_technicians_add
	AMOUNT = $AMOUNT$
}

planet_modifier = {
	zone_building_slots_add = @TINYGROX_MAX_BUILDINGS
}

ai_priority = 5
max_buildings = @TINYGROX_MAX_BUILDINGS
