# Simple More Building Slots: slots -> @TINYGROX_MAX_BUILDINGS, max_buildings cap added if missing (vanilla shared_industrial_factory_zone.txt).
icon = GFX_district_specialization_factory
base_buildtime = @zone_buildtime
potential = { # planet scope
	hidden_trigger ={
		exists = owner
		owner = {
			country_uses_consumer_goods = yes
		}
	}
}

unlock = { # planet scope
	hidden_trigger ={
		exists = owner
		owner = {
			country_uses_consumer_goods = yes
		}
	}
}

resources = {
	category = planet_zones
	inline_script = {
		script = buildings/nomadic_cost_switcher
		REGULAR_RESOURCE = minerals
		NOMADIC_RESOURCE = alloys
		COST = @zone_cost
		NOMADIC_COST_MULT = 0.5
	}
}

included_building_sets = {
	factory
	urban_automation
	origin
}

inline_script = {
	script = jobs/zone_factory_add
	AMOUNT = $AMOUNT$
}

planet_modifier = {
	zone_building_slots_add = @TINYGROX_MAX_BUILDINGS
}

ai_resource_production = {
	consumer_goods = 15

	trigger = {
		has_any_factory_zone = yes
	}
}

ai_priority = 10
max_buildings = @TINYGROX_MAX_BUILDINGS
