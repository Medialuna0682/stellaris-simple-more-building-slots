# Arkship slots patch (简单更多区划建筑槽位): + max_buildings, zone_building_slots_add -> @TINYGROX_MAX_BUILDINGS (was 3)
# Used by arkship zone_ark_reactor_physics (all other users redirected to z_myResearch_physics_zone).
icon = GFX_district_specialization_physics
base_buildtime = @zone_buildtime
unlock = { # planet scope
	hidden_trigger = {
		exists = owner
		owner = {
			is_wilderness_empire = no
		}
	}
}

resources = {
	category = planet_zones
	inline_script = {
		script = buildings/nomadic_cost_switcher
		REGULAR_RESOURCE = minerals
		NOMADIC_RESOURCE = alloys
		COST = @zone_cost
		NOMADIC_COST_MULT = 0.5
	}
}

included_building_sets = {
	physics
	research
	urban_automation
	origin
}

inline_script = {
	script = jobs/zone_physicists_add
	LARGE_AMOUNT = $AMOUNT$
}

planet_modifier = {
	zone_building_slots_add = @TINYGROX_MAX_BUILDINGS		# 3
}
max_buildings = @TINYGROX_MAX_BUILDINGS
