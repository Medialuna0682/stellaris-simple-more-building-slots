# Simple More Building Slots: slots -> @TINYGROX_MAX_BUILDINGS, max_buildings cap added if missing (vanilla shared_ark_forever_cruise_city_passenger_zone.txt).
base_buildtime = @zone_buildtime

potential = { # planet scope
	hidden_trigger = {
		owner? = {
			is_wilderness_empire = no
			has_origin = origin_forever_cruise
			NOT = { has_country_flag = forever_cruise_over }
		}
	}
}

convert_to = {
	zone_urban
}

unlock = { # planet scope
	hidden_trigger = {
		owner? = {
			is_wilderness_empire = no
		}
	}
}

resources = {
	category = planet_zones
	inline_script = {
		script = buildings/nomadic_cost_switcher
		REGULAR_RESOURCE = minerals
		NOMADIC_RESOURCE = alloys
		COST = @zone_cost
		NOMADIC_COST_MULT = 0.5
	}
}

inline_script = {
	script = jobs/cruise_passengers_add
	AMOUNT = $AMOUNT$
}

included_building_sets = {
	urban
	ark_forever_cruise_passengers
}

planet_modifier = {
	zone_building_slots_add = @TINYGROX_MAX_BUILDINGS
}

ai_priority = 5
max_buildings = @TINYGROX_MAX_BUILDINGS
