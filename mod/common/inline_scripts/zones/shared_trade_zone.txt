# Simple More Building Slots: slots -> @TINYGROX_MAX_BUILDINGS, max_buildings cap added if missing (vanilla shared_trade_zone.txt).
icon = GFX_district_specialization_trade
base_buildtime = @zone_buildtime
potential = { # planet scope
	exists = owner
	owner = {
		is_wilderness_empire = no
	}
	OR = {
		owner = {
			is_ai = no
		}
		has_any_research_zone = no
		has_any_industrial_zone = no
	}
}

unlock = { # planet scope
	always = yes
}

resources = {
	category = planet_zones
	inline_script = {
		script = buildings/nomadic_cost_switcher
		REGULAR_RESOURCE = minerals
		NOMADIC_RESOURCE = alloys
		COST = @zone_cost
		NOMADIC_COST_MULT = 0.5
	}
}

included_building_sets = {
	urban
	trade
	urban_automation
	origin
}

inline_script = {
	script = jobs/zone_trader_add
	AMOUNT = $AMOUNT$
}

planet_modifier = {
	zone_building_slots_add = @TINYGROX_MAX_BUILDINGS
}

ai_weight_coefficient = 0.5
ai_priority = 10
max_buildings = @TINYGROX_MAX_BUILDINGS
