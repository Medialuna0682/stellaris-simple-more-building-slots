# Simple More Building Slots: slots -> @TINYGROX_MAX_BUILDINGS, max_buildings cap added if missing (vanilla shared_unity_spiritualist_zone.txt).
icon = GFX_district_specialization_priest
base_buildtime = @zone_buildtime
potential = { # planet scope
	hidden_trigger = { exists = owner }
	owner = {
		is_spiritualist = yes
	}
	OR = {
		owner = {
			is_ai = no
		}
		AND = {
			has_any_research_zone = no
			has_any_industrial_zone = no
		}
	}
}

unlock = { # planet scope
	always = yes
}

resources = {
	category = planet_zones
	inline_script = {
		script = buildings/nomadic_cost_switcher
		REGULAR_RESOURCE = minerals
		NOMADIC_RESOURCE = alloys
		COST = @zone_cost
		NOMADIC_COST_MULT = 0.5
	}
}

included_building_sets = {
	unity
	urban_automation
	origin
}

inline_script = {
	script = jobs/zone_unity_jobs_add
	AMOUNT = $AMOUNT$
}

planet_modifier = {
	zone_building_slots_add = @TINYGROX_MAX_BUILDINGS
}

ai_resource_production = {
	unity = 15

	trigger = {
		has_any_unity_zone = yes
	}
}
ai_priority = 10
max_buildings = @TINYGROX_MAX_BUILDINGS
